# Epic 1 Retrospective: Preview and Trust the Next BMAD Step

## Status

done

## What Changed

- `/bmad-next` now specifies first-run prerequisite diagnostics, dry-run read-only behavior, step selection from workflow/state/artifacts, scope filters, preview output, mutation scope, stop conditions, and failure behavior.
- `/bmad-loop`, README, command reference, and examples now align around BMAD as a prerequisite, state as an index, artifacts/frontmatter as evidence, and dry-run as the first trust path.
- Templates and schemas now carry first-run expectations without adding runtime code or generated assets.
- Story artifacts for 1.1 through 1.4 record implementation, file lists, change logs, self-review, and follow-up status.

## Review Findings

- Initial AI review found the Epic 1 surface consistent with the repository rules: prompt-first, script-light, no BMAD installer behavior, atomic workflow steps, and evidence-backed state.
- No blocking review follow-ups remained after aligning the README, command specs, command reference, examples, templates, and schema descriptions.
- Residual risk: because v1 is prompt-first, behavior is specified rather than executable. JSON syntax validation covers schemas, but command behavior still depends on agent adherence until future runtime validation is justified.

## Action Items for Epic 2

- Define the exact one-step transaction record shape before documenting execution details.
- Specify how task sub-agent outputs include persona, inputs read, outputs produced, and scope-respected self-checks.
- Keep optional-step behavior consistent: include by default, skip only when `--skip-optional` is requested.
- Preserve the Epic 1 trust boundary in execution docs: state updates only after declared evidence passes.
